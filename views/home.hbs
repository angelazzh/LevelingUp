<body>
    <div class="container">
        <header>
             <a href="/" style="text-decoration: none; color: inherit;">
                <img class="logo" src="/images/logo.png" alt="Logo">
                <h1>LEVELING UP</h1>
            </a>
        </header>
        <div class="log">
            {{#if user}}
                <a href="/logout">Logout</a>
            {{else}}
                <a href="/register" class="auth-link-1">Register</a>
                <a href="/login" class="auth-link-2">Login</a>
            {{/if}}
        </div>
        <main>
            <section class="greeting">
                <p>{{greeting}}, {{name}}</p>
                <p>Today is {{date}}</p>
            </section>
            <section class="content">
                {{#if user}}
                    <div class="routines">
                        <a href="/routines">ROUTINES🕒</a>
                    </div>
                    <div class="goals">
                        <a href="/goals">GOALS🥅</a>
                    </div>
                {{else}}
                <p>Please log in to see your routines and goals.</p>
                {{/if}}
            </section>
            {{#if user}}
                 <div class="gymbot">
                    <p>Need help with your fitness journey?</p>
                    <a href="/ask" class="button">ASK AI GYMBOT🏋🏼‍♂️🤖</a>
                </div>
            {{/if}}
        </main>
    </div>
</body>
